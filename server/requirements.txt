fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
numpy>=1.24.0
yt-dlp>=2024.1.0

# Dev / CI dependencies (install with: pip install -r requirements.txt ruff mypy pytest anyio httpx)
# pytest>=8.0
# anyio[trio]>=4.0
# httpx>=0.27
# ruff>=0.4
# mypy>=1.9

# Optional: install Meta's tribev2 from GitHub and its stack (PyTorch, etc.) — see README.
# tribev2 is not on PyPI as a simple pip name; clone https://github.com/facebookresearch/tribev2
